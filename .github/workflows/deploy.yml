name: Deploy to GitHub Pages

on:
  # 当代码推送到 main 分支时触发
  push:
    branches: [ main ]
  # 允许在 Actions 标签页手动点击按钮触发
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout your repository using git
        uses: actions/checkout@v4

      # =================================================
      # 🚑 紧急修复补丁：手动安装依赖
      # 因为 package-lock.json 被删了，这里强制安装一次
      # 这样服务器就不会因为找不到清单而报错了
      # =================================================
      - name: Install dependencies
        run: npm install

      - name: Install, build, and upload your site
        uses: withastro/action@v2

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
