---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="zh-cn">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		
		<style is:inline>
			/* === 1. 必须修复的移动端布局问题 === */
			html, body {
				width: 100%;
				max-width: 100vw;
				overflow-x: hidden; /* 🚫 关键：禁止横向滚动，防止断层 */
				margin: 0;
				padding: 0;
			}

			/* === 2. CSS 纯代码绘制背景 (永不失效) === */
			.fixed-bg {
				position: fixed;
				top: 0;
				left: 0;
				width: 100%;
				height: 100%;
				z-index: -1;
				/* 下面这段代码用 CSS 画出了“晨雾中的山脉” */
				background: 
					/* 顶层的噪点纹理，增加胶片质感 */
					url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)' opacity='0.05'/%3E%3C/svg%3E"),
					/* 迷雾光晕 */
					radial-gradient(circle at 50% 0%, rgba(224, 231, 255, 0.8) 0%, rgba(248, 250, 252, 0) 60%),
					/* 模拟山脉的层次 */
					linear-gradient(180deg, #e0e7ff 0%, #f1f5f9 40%, #cbd5e1 100%);
				background-size: cover;
			}

			/* === 3. 全局字体与排版 === */
			body {
				font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
				color: #1e293b;
				line-height: 1.6;
				background-color: transparent;
			}
			
			/* 容器修复：加上 box-sizing 防止 padding 撑破容器 */
			.container { 
				width: 100%; 
				max-width: 1024px; 
				margin: 0 auto; 
				padding: 0 20px; 
				box-sizing: border-box; 
			}

			/* === 4. Header 修复 (两端对齐) === */
			header {
				width: 100%; /* 确保铺满 */
				background: rgba(255, 255, 255, 0.7);
				backdrop-filter: blur(12px);
				-webkit-backdrop-filter: blur(12px);
				border-bottom: 1px solid rgba(255, 255, 255, 0.6);
				position: sticky;
				top: 0;
				z-index: 50;
				padding: 16px 0;
			}
			/* 修复 Flex 布局在手机上挤成一团的问题 */
			.header-inner {
				display: flex;
				justify-content: space-between;
				align-items: center;
				width: 100%;
			}
			.logo-area { 
				font-weight: 800; 
				font-size: 1.2rem; 
				color: #312e81; 
				display: flex; 
				align-items: center; 
				gap: 8px; 
				white-space: nowrap; /* 防止 Logo 换行 */
			}
			.nav-links a {
				color: #475569;
				text-decoration: none;
				font-size: 0.9rem;
				font-weight: 600;
				margin-left: 20px;
			}
			
			/* === 5. 卡片样式 (磨砂感增强) === */
			.glass-card {
				background: rgba(255, 255, 255, 0.65); /* 透出更多背景 */
				backdrop-filter: blur(16px);
				-webkit-backdrop-filter: blur(16px);
				border: 1px solid rgba(255, 255, 255, 0.8);
				box-shadow: 0 8px 32px rgba(30, 41, 59, 0.05);
				border-radius: 24px;
				padding: 40px;
				margin-bottom: 40px;
				width: 100%; /* 确保卡片不溢出 */
				box-sizing: border-box;
			}

			/* Hero 文字 */
			.hero-section { padding: 60px 20px; text-align: center; }
			h1 {
				font-size: 2.8rem;
				font-weight: 900;
				color: #1e293b;
				margin: 0 0 20px;
				letter-spacing: -0.02em;
			}
			.text-indigo { color: #4f46e5; }

			/* 公式 Grid 适配 */
			.formula-grid { 
				display: flex; 
				gap: 20px; 
				margin-top: 30px; 
				flex-wrap: wrap; /* 允许换行 */
			}
			.formula-item {
				flex: 1;
				min-width: 200px; /* 手机上会自动变成一列 */
				background: rgba(255, 255, 255, 0.5);
				border: 1px solid rgba(255,255,255,0.6);
				padding: 24px;
				border-radius: 16px;
				text-align: center;
			}
			.multiply { font-size: 24px; color: #94a3b8; display: flex; align-items: center; justify-content: center; }

			/* 视频卡片 */
			.video-card {
				background: #0f172a;
				border-radius: 24px;
				padding: 32px;
				color: white;
				margin-bottom: 40px;
				position: relative;
				overflow: hidden;
				box-shadow: 0 20px 40px -10px rgba(15, 23, 42, 0.3);
			}
			.video-btn {
				display: inline-block; background: #4f46e5; color: white; 
				padding: 10px 20px; border-radius: 8px; text-decoration: none; 
				margin-top: 20px; font-size: 0.9rem; font-weight: 600;
			}

			/* CTA 区域 */
			.cta-section {
				background: linear-gradient(135deg, #4338ca 0%, #6d28d9 100%);
				padding: 48px 24px;
				border-radius: 24px;
				text-align: center;
				color: white;
				margin-top: 60px;
				box-shadow: 0 20px 40px rgba(67, 56, 202, 0.25);
			}
			.cta-btn {
				background: white; color: #4338ca; padding: 14px 32px; 
				border-radius: 12px; font-weight: bold; text-decoration: none; 
				display: inline-flex; align-items: center; gap: 8px; margin-top: 24px;
			}

			/* === 📱 移动端专项修复 === */
			@media (max-width: 768px) {
				h1 { font-size: 2rem; }
				.glass-card { padding: 24px; } /* 减小内边距 */
				.formula-grid { flex-direction: column; gap: 12px; }
				.multiply { transform: rotate(90deg); height: 20px; } /* 乘号旋转 */
				
				/* 隐藏导航链接，只留 Logo 和 CTA */
				.nav-links { display: none; } 
				
				/* 让 Footer 和 Header 撑满 */
				header, footer { width: 100%; }
				
				.cta-section { padding: 32px 20px; }
				.hero-desc { font-size: 1rem; }
			}
		</style>
	</head>
	<body>
		
		<!-- 🌌 背景层：纯 CSS 绘制的迷雾山脉 -->
		<div class="fixed-bg"></div>

		<header>
			<div class="container header-inner">
				<div class="logo-area">
					<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76"/></svg>
					<span>Logan制作</span>
				</div>
				<nav class="nav-links">
					<a href="#" class="active">核心理念</a>
					<a href="/guide">开始探索</a>
				</nav>
			</div>
		</header>

		<main class="container" style="padding-bottom: 80px;">
			
			<!-- Hero Section -->
			<section class="hero-section">
				<div style="display:inline-block; background:rgba(224, 231, 255, 0.6); color:#3730a3; padding:4px 12px; border-radius:50px; font-size:12px; font-weight:700; margin-bottom:24px; border:1px solid rgba(255,255,255,0.5);">
					✨ 基于八木仁平自我认知方法论
				</div>
				<h1>不要只是努力，<br class="md:hidden" /><span class="text-indigo">做出正确的选择</span></h1>
				<p class="hero-desc" style="color: #475569; max-width: 600px; margin: 0 auto;">
					建立这个网站的初衷，是深受老高视频启发。
					AI 助手将引导你生成属于你的“人生使用说明书”。
				</p>
			</section>

			<!-- Formula Card -->
			<section class="glass-card">
				<h2 style="text-align:center; margin-top:0; color:#334155;">真正的人生之路公式</h2>
				<div class="formula-grid">
					<div class="formula-item">
						<div style="color:#be123c; font-weight:bold; font-size:1.1rem;">❤️ 热情</div>
						<div style="font-size:12px; color:#881337; opacity:0.8;">喜欢的、感兴趣的</div>
					</div>
					<div class="multiply">×</div>
					<div class="formula-item">
						<div style="color:#b45309; font-weight:bold; font-size:1.1rem;">⚡ 才能</div>
						<div style="font-size:12px; color:#78350f; opacity:0.8;">擅长的、无意识做的</div>
					</div>
					<div class="multiply">×</div>
					<div class="formula-item">
						<div style="color:#0e7490; font-weight:bold; font-size:1.1rem;">💎 价值观</div>
						<div style="font-size:12px; color:#164e63; opacity:0.8;">在乎的、底线</div>
					</div>
				</div>
				<div style="text-align:center; margin-top:24px; padding-top:20px; border-top:1px solid rgba(0,0,0,0.05); font-weight:bold; color:#4338ca;">
					= 真正的人生之路
				</div>
			</section>

			<!-- Video Section -->
			<div class="video-card">
				<h3 style="margin:0 0 12px; font-size:1.4rem;">找到你的“人生之路”</h3>
				<p style="color:#94a3b8; margin-bottom:20px; font-size:0.9rem;">这一期你应该多看几遍 | 老高与小茉</p>
				<a href="https://b23.tv/Pedon5x" target="_blank" class="video-btn">
					📺 观看完整视频 (Bilibili)
				</a>
			</div>

			<!-- Content Summary -->
			<section class="glass-card">
				<h3 style="margin-top:0; display:flex; align-items:center; gap:10px; color:#312e81;">
					<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path><polyline points="14 2 14 8 20 8"></polyline><line x1="16" y1="13" x2="8" y2="13"></line><line x1="16" y1="17" x2="8" y2="17"></line><polyline points="10 9 9 9 8 9"></polyline></svg>
					深度解析
				</h3>
				<ul style="padding-left:0; list-style:none; margin-top:20px;">
					<li style="margin-bottom:16px; display:flex; gap:12px;">
						<span style="color:#10b981;">✓</span>
						<span style="font-size:0.95rem;"><strong>打破误区：</strong> 很多人痛苦不是因为不努力，而是在错误的方向死磕。</span>
					</li>
					<li style="margin-bottom:16px; display:flex; gap:12px;">
						<span style="color:#10b981;">✓</span>
						<span style="font-size:0.95rem;"><strong>过滤器思维：</strong> 用价值观把“错的”筛掉，剩下的就是路。</span>
					</li>
					<li style="display:flex; gap:12px;">
						<span style="color:#10b981;">✓</span>
						<span style="font-size:0.95rem;"><strong>治愈焦虑：</strong> 你的缺点往往就是你的天赋。</span>
					</li>
				</ul>
			</section>

			<!-- CTA -->
			<section class="cta-section">
				<h2 style="margin:0 0 16px; font-size: 1.8rem;">准备好编写你的说明书了吗？</h2>
				<p style="opacity: 0.9; font-size: 1rem; margin-bottom: 0;">
					我们的 AI 助手将引导你逐一回答关键问题。
				</p>
				<a href="/guide" class="cta-btn">
					🚀 开始 AI 引导式探索
				</a>
			</section>

		</main>
		
		<Footer />
	</body>
</html>
