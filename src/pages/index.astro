---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="zh-cn">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		
		<!-- 强制移动端视口设置，防止页面被缩小 -->
		<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0" />

		<style is:inline>
			/* === 1. 布局核弹级修复 (保持不变，防止断层) === */
			* {
				box-sizing: border-box;
				max-width: 100%;
			}
			html, body {
				margin: 0;
				padding: 0;
				width: 100%;
				overflow-x: hidden;
				font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
				color: #1e293b;
			}

			/* === 2. 背景图设置 (保持你刚才满意的效果) === */
			.fixed-background {
				position: fixed;
				top: 0;
				left: 0;
				width: 100vw;
				height: 100vh;
				z-index: -1;
				/* 迷雾山脉背景图 */
				background-image: url('https://images.unsplash.com/photo-1464822759023-fed622ff2c3b?q=80&w=1200&auto=format&fit=crop');
				background-size: cover;
				background-position: center;
			}
			.fixed-overlay {
				position: fixed;
				top: 0;
				left: 0;
				width: 100vw;
				height: 100vh;
				z-index: -1;
				background: rgba(248, 250, 252, 0.5); /* 半透明遮罩 */
				backdrop-filter: blur(3px);
			}

			/* === 3. 通用容器 === */
			.container {
				width: 100%;
				max-width: 1024px;
				margin: 0 auto;
				padding: 0 20px;
				position: relative;
			}

			/* Header */
			header {
				background: rgba(255, 255, 255, 0.85);
				backdrop-filter: blur(12px);
				border-bottom: 1px solid rgba(0,0,0,0.05);
				position: sticky;
				top: 0;
				z-index: 50;
				padding: 16px 0;
			}
			.header-inner { display: flex; justify-content: space-between; align-items: center; }
			.logo-area { font-weight: 800; font-size: 1.2rem; color: #4338ca; display: flex; align-items: center; gap: 8px; }
			.nav-links { display: flex; gap: 24px; }
			.nav-links a { color: #64748b; text-decoration: none; font-weight: 600; font-size: 0.9rem; }
			.nav-links a.active { color: #4f46e5; }

			/* === 4. 卡片样式 (统一磨砂风) === */
			.glass-card {
				background: rgba(255, 255, 255, 0.75); /* 稍微调高一点点不透明度，保证文字可读性 */
				backdrop-filter: blur(16px);
				border-radius: 24px;
				border: 1px solid rgba(255, 255, 255, 0.6);
				box-shadow: 0 8px 30px rgba(0, 0, 0, 0.06);
				padding: 40px;
				margin-bottom: 40px;
			}

			/* Hero 区域 */
			.hero-section { padding: 80px 0 60px; text-align: center; }
			.badge {
				display: inline-flex; align-items: center; gap: 6px;
				background: rgba(224, 231, 255, 0.8); color: #3730a3;
				padding: 6px 16px; border-radius: 99px;
				font-size: 13px; font-weight: 700; margin-bottom: 24px;
			}
			h1 { font-size: 3rem; margin: 0 0 24px; line-height: 1.2; color: #0f172a; font-weight: 900; }
			.text-indigo { color: #4f46e5; }
			.hero-desc { font-size: 1.1rem; color: #475569; max-width: 650px; margin: 0 auto; line-height: 1.75; }

			/* 公式区域 (恢复原版设计) */
			.formula-grid { display: flex; align-items: center; justify-content: center; gap: 20px; margin-top: 32px; flex-wrap: wrap; }
			.formula-item {
				flex: 1; min-width: 200px;
				background: rgba(255,255,255,0.6);
				padding: 24px; border-radius: 16px; text-align: center;
				border: 1px solid rgba(255,255,255,0.5);
			}
			.text-rose { color: #be123c; } .text-amber { color: #b45309; } .text-cyan { color: #0e7490; }
			.icon-lg { width: 32px; height: 32px; margin-bottom: 12px; }
			.multiply { font-size: 24px; color: #94a3b8; display: flex; justify-content: center; }
			.result-line { margin-top: 32px; padding-top: 24px; border-top: 1px solid rgba(0,0,0,0.05); text-align: center; font-size: 1.25rem; font-weight: 700; color: #1e293b; }

			/* 视频卡片 (恢复深色设计) */
			.video-card {
				background: #0f172a; color: white;
				border-radius: 20px; padding: 40px; margin-bottom: 40px;
				position: relative; overflow: hidden;
				box-shadow: 0 20px 40px -10px rgba(15, 23, 42, 0.3);
			}
			.video-btn {
				display: inline-flex; align-items: center; gap: 8px;
				background: #4f46e5; color: white;
				padding: 12px 24px; border-radius: 10px;
				text-decoration: none; margin-top: 24px; font-weight: 600;
				transition: transform 0.2s;
			}
			
			/* 核心价值列表 (恢复原版内容) */
			.section-title { font-size: 1.5rem; font-weight: 700; margin: 0 0 24px; display: flex; align-items: center; gap: 12px; color: #1e293b; }
			.info-box { background: rgba(224, 231, 255, 0.5); padding: 24px; border-radius: 12px; margin-bottom: 32px; border: 1px solid rgba(199, 210, 254, 0.5); }
			.check-list { padding: 0; list-style: none; }
			.check-list li { display: flex; gap: 16px; margin-bottom: 20px; align-items: flex-start; }
			.check-icon { color: #10b981; width: 24px; height: 24px; flex-shrink: 0; margin-top: 2px; }

			/* CTA 底部 (恢复原版) */
			.cta-section {
				background: linear-gradient(135deg, #4f46e5 0%, #7e22ce 100%);
				padding: 60px 30px; border-radius: 24px; text-align: center; color: white; margin-top: 60px;
				box-shadow: 0 25px 50px -12px rgba(79, 70, 229, 0.4);
			}
			.cta-btn-lg {
				background: white; color: #4f46e5; padding: 16px 32px;
				border-radius: 12px; font-size: 1.125rem; font-weight: 700;
				text-decoration: none; display: inline-flex; align-items: center; gap: 8px;
				margin-top: 32px; box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
			}

			/* 📱 移动端适配 */
			@media (max-width: 768px) {
				h1 { font-size: 2.25rem; }
				.nav-links { display: none; } /* 只显示Logo */
				.glass-card { padding: 24px; }
				.formula-grid { flex-direction: column; }
				.multiply { transform: rotate(90deg); margin: 8px 0; }
				.hero-desc { font-size: 1rem; }
			}
		</style>
	</head>
	<body>
		
		<!-- 🌌 背景层 -->
		<div class="fixed-background"></div>
		<div class="fixed-overlay"></div>

		<header>
			<div class="container header-inner">
				<div class="logo-area">
					<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76"/></svg>
					<span>Logan制作</span>
				</div>
				<nav class="nav-links">
					<a href="#" class="active">核心理念</a>
					<a href="/guide">开始探索</a>
				</nav>
			</div>
		</header>

		<main class="container" style="padding-bottom: 80px;">
			
			<!-- Hero -->
			<section class="hero-section">
				<div class="badge">
					<svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m12 3-1.912 5.813a2 2 0 0 1-1.275 1.275L3 12l5.813 1.912a2 2 0 0 1 1.275 1.275L12 21l1.912-5.813a2 2 0 0 1 1.275-1.275L21 12l-5.813-1.912a2 2 0 0 1-1.275-1.275L12 3Z"/></svg>
					基于八木仁平自我认知方法论
				</div>
				<h1>不要只是努力，<br class="md:hidden" /><span class="text-indigo">做出正确的选择</span></h1>
				<p class="hero-desc">
					建立这个网站的初衷，是深受老高视频中介绍的八木仁平《做自己擅长的事，才是人生的真谛》启发。
					<br class="hidden md:block"/>
					我们希望利用 <strong>AI 技术</strong>，将这套“系统化”的理论转化为一个互动的引导过程，
					帮助你自动生成属于你的<strong>“人生使用说明书”</strong>。
				</p>
			</section>

			<!-- Formula Card -->
			<section class="glass-card">
				<h2 style="text-align:center; margin-top:0;">真正的人生之路公式</h2>
				<div class="formula-grid">
					<!-- Passion -->
					<div class="formula-item">
						<svg class="icon-lg text-rose" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.3 1.5 4.05 3 5.5l7 7Z"/></svg>
						<h3 class="text-rose" style="margin:0;">热情 (Passion)</h3>
						<p class="text-rose" style="font-size:13px; margin-top:4px; opacity:0.9;">喜欢的、感兴趣的</p>
					</div>
					<span class="multiply">×</span>
					<!-- Talents -->
					<div class="formula-item">
						<svg class="icon-lg text-amber" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>
						<h3 class="text-amber" style="margin:0;">才能 (Talents)</h3>
						<p class="text-amber" style="font-size:13px; margin-top:4px; opacity:0.9;">擅长的、无意识做的</p>
					</div>
					<span class="multiply">×</span>
					<!-- Values -->
					<div class="formula-item">
						<svg class="icon-lg text-cyan" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="6"/><circle cx="12" cy="12" r="2"/></svg>
						<h3 class="text-cyan" style="margin:0;">价值观 (Values)</h3>
						<p class="text-cyan" style="font-size:13px; margin-top:4px; opacity:0.9;">在乎的、底线</p>
					</div>
				</div>
				<div class="result-line">
					= <span class="text-indigo">真正的人生之路</span>
				</div>
			</section>

			<!-- Video Card -->
			<div class="video-card">
				<h3 style="font-size:1.5rem; margin:0 0 8px;">找到你的“人生之路”的正確方法</h3>
				<p style="color:#cbd5e1; margin-bottom:24px;">这一期你应该多看几遍 | 老高与小茉 Mr & Mrs Gao</p>
				<a href="https://b23.tv/Pedon5x" target="_blank" rel="noopener noreferrer" class="video-btn">
					<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polygon points="10 8 16 12 10 16 10 8"/></svg>
					观看完整视频 (Bilibili)
				</a>
			</div>

			<!-- Content Summary (核心价值深度解析 - 恢复版) -->
			<section class="glass-card">
				<div class="section-title text-indigo">
					<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/></svg>
					视频核心价值深度解析
				</div>

				<div class="info-box">
					<p style="font-weight:bold; color:#312e81; margin:0 0 8px;">💡 核心价值</p>
					<p style="margin:0; font-size:0.95rem;">这个视频提供了一套<strong>“系统化、可操作”的自我认知方法论</strong>，帮助那些感到迷茫的人找到适合自己的生活方式。</p>
				</div>

				<ul class="check-list">
					<li>
						<svg class="check-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
						<div>
							<strong style="display:block; margin-bottom:4px;">打破误区</strong> 
							<span style="font-size:0.95rem; color:#475569;">告诉你<strong>“选择比努力更重要”</strong>。很多痛苦是因为在错误的方向死磕。</span>
						</div>
					</li>
					<li>
						<svg class="check-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
						<div>
							<strong style="display:block; margin-bottom:4px;">过滤器思维</strong> 
							<span style="font-size:0.95rem; color:#475569;">提供了一种<strong>“筛选”思维</strong>——与其大海捞针，不如用价值观把“错的”筛掉。</span>
						</div>
					</li>
					<li>
						<svg class="check-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
						<div>
							<strong style="display:block; margin-bottom:4px;">治愈焦虑</strong> 
							<span style="font-size:0.95rem; color:#475569;">你的缺点往往就是你的天赋（例如：反应慢 = 深度专注）。你不必完美，只需要找到发挥“出厂设置”的地方。</span>
						</div>
					</li>
				</ul>
			</section>

			<!-- CTA -->
			<section class="cta-section">
				<h2 style="font-size: 2rem; margin:0 0 16px;">准备好编写你的“人生使用说明书”了吗？</h2>
				<p style="color: #e0e7ff; max-width: 600px; margin: 0 auto; font-size:1.1rem;">
					不需要面对白纸发愁。我们的 AI 助手将引导你逐一回答价值观、才能和热情的关键问题。
				</p>
				<a href="/guide" class="cta-btn-lg">
					开始 AI 引导式探索
					<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
				</a>
			</section>

		</main>
		
		<Footer />
	</body>
</html>
