---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="zh-cn">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		
		<style is:inline>
			/* === 1. 核心背景层设置 (最关键的部分) === */
			.fixed-bg {
				position: fixed;
				top: 0;
				left: 0;
				width: 100vw;
				height: 100vh;
				z-index: -1; /* 放在最底层 */
				
				/* 背景图：选用了一张冷色调的迷雾山脉，非常适合思考人生 */
				background-image: url('https://images.unsplash.com/photo-1519681393798-3828fb4090bb?q=80&w=2574&auto=format&fit=crop');
				background-size: cover;
				background-position: center;
				background-repeat: no-repeat;
			}
			
			/* 给背景加一层半透明遮罩，防止图片太花影响文字阅读 */
			.fixed-bg::after {
				content: '';
				position: absolute;
				top: 0;
				left: 0;
				width: 100%;
				height: 100%;
				background: rgba(248, 250, 252, 0.65); /* 65%透明度的白色遮罩 */
				backdrop-filter: blur(2px); /* 给背景本身也加一点点模糊，更有景深感 */
			}

			/* === 2. 全局排版 === */
			body {
				font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
				color: #1e293b;
				margin: 0;
				line-height: 1.6;
				background: transparent; /* 确保body透明，才能看到底下的背景层 */
			}
			
			.container { max-width: 1024px; margin: 0 auto; padding: 0 20px; }
			.flex { display: flex; }
			.items-center { align-items: center; }
			.justify-between { justify-content: space-between; }
			
			/* === 3. 顶部导航 (磨砂更强) === */
			header {
				background: rgba(255, 255, 255, 0.75); /* 更通透 */
				backdrop-filter: blur(16px);
				-webkit-backdrop-filter: blur(16px);
				border-bottom: 1px solid rgba(255, 255, 255, 0.5);
				position: sticky;
				top: 0;
				z-index: 50;
				padding: 16px 0;
				box-shadow: 0 4px 30px rgba(0, 0, 0, 0.03);
			}
			.logo-area { font-weight: bold; font-size: 1.25rem; color: #4338ca; display: flex; align-items: center; gap: 8px; }
			.nav-links a {
				color: #475569;
				text-decoration: none;
				font-size: 0.875rem;
				font-weight: 600;
				margin-left: 24px;
				transition: color 0.2s;
			}
			.nav-links a:hover { color: #4f46e5; }

			/* === 4. Hero 标题区域 === */
			.hero-section { padding: 100px 20px 80px; text-align: center; }
			h1 {
				font-size: 3.5rem;
				font-weight: 800;
				color: #1e293b;
				line-height: 1.1;
				margin: 0 0 24px;
				/* 文字阴影，确保在任何背景上都清晰 */
				text-shadow: 0 2px 20px rgba(255, 255, 255, 0.8); 
			}
			.text-indigo { 
				background: linear-gradient(135deg, #4f46e5 0%, #7c3aed 100%);
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
			}
			.hero-desc {
				font-size: 1.2rem;
				color: #334155;
				max-width: 650px;
				margin: 0 auto;
				font-weight: 500;
				text-shadow: 0 1px 1px rgba(255,255,255,0.5);
			}

			/* === 5. 磨砂玻璃卡片 (Glassmorphism) === */
			.glass-card {
				/* 关键：背景调得更透明 (0.7)，让背景图的山脉隐约透出来 */
				background: rgba(255, 255, 255, 0.70); 
				backdrop-filter: blur(20px); /* 强模糊，制造这种高级的高斯模糊感 */
				-webkit-backdrop-filter: blur(20px);
				
				border-radius: 24px;
				/* 边框也是半透明的 */
				border: 1px solid rgba(255, 255, 255, 0.6);
				/* 阴影更柔和 */
				box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.1);
				
				padding: 48px;
				margin-bottom: 48px;
			}

			/* 公式部分 */
			.formula-grid { display: flex; justify-content: center; align-items: center; gap: 20px; flex-wrap: wrap; margin-top: 30px; }
			.formula-item {
				flex: 1; min-width: 220px; padding: 30px; border-radius: 16px; text-align: center;
				/* 子元素也用半透明，增加层次感 */
				background: rgba(255, 255, 255, 0.6);
				border: 1px solid rgba(255, 255, 255, 0.5);
				transition: transform 0.3s ease;
			}
			.formula-item:hover { transform: translateY(-5px); background: rgba(255, 255, 255, 0.9); }
			
			.text-rose { color: #be123c; } .text-amber { color: #b45309; } .text-cyan { color: #0e7490; }
			.icon-lg { width: 40px; height: 40px; margin-bottom: 10px; }
			.multiply { font-size: 30px; color: #94a3b8; opacity: 0.6; }
			.result-line { margin-top: 30px; padding-top: 20px; border-top: 1px solid rgba(0,0,0,0.05); text-align: center; font-size: 1.4rem; font-weight: bold; color: #334155; }

			/* 视频卡片 (保持深色，形成视觉锚点) */
			.video-card {
				background: rgba(15, 23, 42, 0.95); /* 深蓝黑色 */
				border-radius: 20px;
				padding: 40px;
				color: white;
				margin-bottom: 40px;
				box-shadow: 0 20px 40px -5px rgba(0,0,0,0.2);
				position: relative;
				overflow: hidden;
			}
			.video-btn {
				display: inline-block; background: #6366f1; color: white; padding: 12px 24px; 
				border-radius: 10px; text-decoration: none; margin-top: 20px; font-weight: 600;
				transition: all 0.2s;
			}
			.video-btn:hover { background: #4f46e5; transform: scale(1.02); }

			/* 详情列表 */
			.section-title { font-size: 1.5rem; font-weight: 800; margin-bottom: 20px; display: flex; align-items: center; gap: 10px; color: #1e293b; }
			.info-box {
				background: rgba(224, 231, 255, 0.5); border: 1px solid rgba(199, 210, 254, 0.5);
				padding: 20px; border-radius: 12px; margin-bottom: 30px;
			}
			.check-list li { margin-bottom: 15px; display: flex; gap: 12px; align-items: flex-start; }
			
			/* CTA 底部 */
			.cta-section {
				background: linear-gradient(135deg, rgba(79, 70, 229, 0.9), rgba(124, 58, 237, 0.9));
				backdrop-filter: blur(10px);
				border-radius: 24px; padding: 60px 30px; text-align: center; color: white; margin-top: 60px;
				box-shadow: 0 25px 50px -12px rgba(79, 70, 229, 0.4);
			}
			.cta-btn {
				background: white; color: #4f46e5; padding: 16px 36px; border-radius: 12px;
				font-size: 1.2rem; font-weight: 800; text-decoration: none; display: inline-flex; 
				align-items: center; gap: 8px; margin-top: 30px;
				box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
				transition: transform 0.2s;
			}
			.cta-btn:hover { transform: translateY(-3px); box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.2); }

			@media (max-width: 768px) {
				h1 { font-size: 2.5rem; }
				.formula-grid { flex-direction: column; }
				.multiply { transform: rotate(90deg); margin: 10px 0; }
			}
		</style>
	</head>
	<body>
		
		<!-- 👇 1. 这里就是神奇的固定背景层 👇 -->
		<div class="fixed-bg"></div>

		<header>
			<div class="container flex justify-between items-center">
				<div class="logo-area">
					<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76"/></svg>
					<span>Logan制作</span>
				</div>
				<nav class="nav-links">
					<a href="#" class="active">核心理念</a>
					<a href="/guide">开始探索</a>
				</nav>
			</div>
		</header>

		<main class="container" style="padding-bottom: 80px;">
			
			<!-- Hero -->
			<section class="hero-section">
				<div style="display:inline-block; background:rgba(224, 231, 255, 0.8); color:#4338ca; padding:5px 15px; border-radius:50px; font-size:13px; font-weight:bold; margin-bottom:20px; backdrop-filter:blur(4px);">
					✨ 基于八木仁平自我认知方法论
				</div>
				<h1>不要只是努力，<br class="md:hidden" /><span class="text-indigo">做出正确的选择</span></h1>
				<p class="hero-desc">
					建立这个网站的初衷，是深受老高视频中介绍的八木仁平《做自己擅长的事，才是人生的真谛》启发。
					AI 助手将引导你生成属于你的“人生使用说明书”。
				</p>
			</section>

			<!-- Formula Card -->
			<section class="glass-card">
				<h2 class="text-center" style="margin-top:0; font-size:1.8rem; color:#1e293b;">真正的人生之路公式</h2>
				<div class="formula-grid">
					<div class="formula-item">
						<svg class="icon-lg text-rose" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.3 1.5 4.05 3 5.5l7 7Z"/></svg>
						<h3 class="text-rose" style="margin:0;">热情 (Passion)</h3>
						<p style="font-size:13px; margin-top:5px; opacity:0.8;">喜欢的、感兴趣的</p>
					</div>
					<span class="multiply">×</span>
					<div class="formula-item">
						<svg class="icon-lg text-amber" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>
						<h3 class="text-amber" style="margin:0;">才能 (Talents)</h3>
						<p style="font-size:13px; margin-top:5px; opacity:0.8;">擅长的、无意识做的</p>
					</div>
					<span class="multiply">×</span>
					<div class="formula-item">
						<svg class="icon-lg text-cyan" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="6"/><circle cx="12" cy="12" r="2"/></svg>
						<h3 class="text-cyan" style="margin:0;">价值观 (Values)</h3>
						<p style="font-size:13px; margin-top:5px; opacity:0.8;">在乎的、底线</p>
					</div>
				</div>
				<div class="result-line">
					= <span style="color:#4f46e5;">真正的人生之路</span>
				</div>
			</section>

			<!-- Video Section -->
			<div class="video-card">
				<div style="position:relative; z-index:1;">
					<h3 style="font-size:1.6rem; margin:0 0 10px;">找到你的“人生之路”的正確方法</h3>
					<p style="color:#94a3b8; margin-bottom:25px;">这一期你应该多看几遍 | 老高与小茉 Mr & Mrs Gao</p>
					<a href="https://b23.tv/Pedon5x" target="_blank" class="video-btn">
						📺 观看完整视频 (Bilibili)
					</a>
				</div>
			</div>

			<!-- Content Summary -->
			<section class="glass-card">
				<div class="section-title">
					<svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#4f46e5" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/></svg>
					视频核心价值深度解析
				</div>

				<div class="info-box">
					<p style="margin:0;">💡 <strong>核心价值：</strong>将模糊的“寻找人生目标”具体化为三个清晰的圆圈：<strong>价值观、才能、热情</strong>。解决“迷茫”与“内耗”。</p>
				</div>

				<ul class="check-list" style="padding:0; list-style:none;">
					<li>
						<svg style="color:#10b981; width:24px; height:24px; flex-shrink:0;" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
						<span><strong>打破误区：</strong> “选择比努力更重要”。很多人痛苦不是因为不努力，而是在错误的方向死磕。</span>
					</li>
					<li>
						<svg style="color:#10b981; width:24px; height:24px; flex-shrink:0;" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
						<span><strong>过滤器思维：</strong> 与其大海捞针找“对的”，不如先用价值观把“错的”筛掉，剩下的就是路。</span>
					</li>
					<li>
						<svg style="color:#10b981; width:24px; height:24px; flex-shrink:0;" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
						<span><strong>治愈焦虑：</strong> 你的缺点往往就是你的天赋（例如：反应慢 = 深度专注）。</span>
					</li>
				</ul>
			</section>

			<!-- CTA -->
			<section class="cta-section">
				<h2 style="font-size: 2.2rem; margin:0 0 20px;">准备好编写你的“人生使用说明书”了吗？</h2>
				<p style="opacity: 0.9; max-width: 600px; margin: 0 auto; font-size: 1.1rem;">
					不需要面对白纸发愁。我们的 AI 助手将引导你逐一回答关键问题。
				</p>
				<a href="/guide" class="cta-btn">
					🚀 开始 AI 引导式探索
				</a>
			</section>

		</main>
		
		<Footer />
	</body>
</html>
