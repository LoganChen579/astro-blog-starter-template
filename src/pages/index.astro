---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="zh-cn">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		
		<!-- 强制移动端视口设置，防止页面被缩小 -->
		<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0" />

		<style is:inline>
			/* === 1. 核弹级 CSS 重置 (解决手机断层) === */
			* {
				box-sizing: border-box; /* 所有的边框和内边距都不会撑大盒子 */
				max-width: 100%; /* 所有的元素都不准超过屏幕宽 */
			}

			html, body {
				margin: 0;
				padding: 0;
				width: 100%;
				overflow-x: hidden; /* 🚫 禁止横向滚动，彻底消灭右侧白边 */
				font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
				color: #1e293b;
			}

			/* === 2. 独立的背景层 (z-index: -1) === */
			.fixed-background {
				position: fixed;
				top: 0;
				left: 0;
				width: 100vw;
				height: 100vh;
				z-index: -1;
				
				/* 👇 使用 Unsplash 高清源，稳定性极高，必定能加载出来 */
				background-image: url('https://images.unsplash.com/photo-1464822759023-fed622ff2c3b?q=80&w=1200&auto=format&fit=crop');
				background-size: cover;
				background-position: center;
			}
			
			/* 这一层是用来给背景加“雾气”的，防止字看不清 */
			.fixed-overlay {
				position: fixed;
				top: 0;
				left: 0;
				width: 100vw;
				height: 100vh;
				z-index: -1;
				background: rgba(248, 250, 252, 0.5); /* 50% 的白色遮罩，透出背景 */
				backdrop-filter: blur(3px);
			}

			/* === 3. 容器与布局 === */
			.container {
				width: 100%;
				max-width: 1024px;
				margin: 0 auto;
				padding: 0 20px;
				position: relative; /* 确保在背景之上 */
			}

			/* Header */
			header {
				background: rgba(255, 255, 255, 0.85);
				backdrop-filter: blur(12px);
				border-bottom: 1px solid rgba(0,0,0,0.05);
				position: sticky;
				top: 0;
				z-index: 50;
				padding: 12px 0;
			}
			.header-inner {
				display: flex;
				justify-content: space-between;
				align-items: center;
			}
			.logo-area {
				font-weight: 800;
				font-size: 1.1rem;
				color: #4338ca;
				display: flex;
				align-items: center;
				gap: 8px;
			}
			/* 手机上强制隐藏导航链接，防止挤压 Logo */
			.nav-links { display: flex; gap: 20px; }
			.nav-links a { color: #64748b; text-decoration: none; font-weight: 600; font-size: 0.9rem; }
			
			/* === 4. 卡片样式 (磨砂质感) === */
			.glass-card {
				background: rgba(255, 255, 255, 0.7); /* 70% 不透明，透出山脉 */
				backdrop-filter: blur(16px);
				border-radius: 20px;
				border: 1px solid rgba(255, 255, 255, 0.6);
				box-shadow: 0 8px 30px rgba(0, 0, 0, 0.06);
				padding: 30px;
				margin-bottom: 30px;
			}

			/* Hero */
			.hero-section { padding: 60px 0 40px; text-align: center; }
			h1 { font-size: 2.5rem; margin: 0 0 20px; line-height: 1.2; color: #0f172a; font-weight: 900; }
			.text-indigo { color: #4f46e5; }

			/* Grid */
			.formula-grid { display: flex; align-items: center; justify-content: center; gap: 15px; margin-top: 30px; flex-wrap: wrap; }
			.formula-item {
				flex: 1; 
				min-width: 180px; /* 手机上自动换行 */
				background: rgba(255,255,255,0.6); 
				padding: 20px; 
				border-radius: 12px; 
				text-align: center;
			}
			.multiply { font-size: 20px; color: #94a3b8; }

			/* Video Card */
			.video-card {
				background: #1e1b4b; /* 深靛蓝 */
				color: white;
				border-radius: 20px;
				padding: 30px;
				margin-bottom: 30px;
				box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
			}
			.video-btn {
				display: inline-block; background: #6366f1; color: white; padding: 10px 20px; 
				border-radius: 8px; text-decoration: none; margin-top: 15px; font-weight: bold;
			}

			/* CTA */
			.cta-section {
				background: linear-gradient(to right, #4f46e5, #7c3aed);
				color: white;
				padding: 40px 20px;
				border-radius: 20px;
				text-align: center;
				margin-top: 50px;
			}
			.cta-btn {
				display: inline-block; background: white; color: #4f46e5; 
				padding: 14px 30px; border-radius: 12px; text-decoration: none; 
				font-weight: bold; margin-top: 20px;
				box-shadow: 0 4px 6px rgba(0,0,0,0.1);
			}

			/* === 📱 针对手机的专项修复 === */
			@media (max-width: 768px) {
				h1 { font-size: 2rem; } /* 缩小标题 */
				.nav-links { display: none; } /* 隐藏导航 */
				.glass-card { padding: 20px; } /* 减小内边距 */
				.hero-section { padding: 40px 0 30px; }
				.formula-grid { flex-direction: column; } /* 竖排 */
				.multiply { transform: rotate(90deg); margin: 5px 0; } /* 旋转乘号 */
				
				/* 修复 Logo 挤压 */
				.logo-area span { font-size: 1rem; }
			}
		</style>
	</head>
	<body>
		
		<!-- 🌌 1. 背景图 (绝对定位，永远在最底层) -->
		<div class="fixed-background"></div>
		<div class="fixed-overlay"></div>

		<header>
			<div class="container header-inner">
				<div class="logo-area">
					<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76"/></svg>
					<span>Logan制作</span>
				</div>
				<nav class="nav-links">
					<a href="#">核心理念</a>
					<a href="/guide" style="color:#4f46e5;">开始探索</a>
				</nav>
			</div>
		</header>

		<main class="container" style="padding-bottom: 80px;">
			
			<!-- Hero -->
			<section class="hero-section">
				<div style="display:inline-block; background:rgba(224,231,255,0.8); color:#3730a3; padding:4px 12px; border-radius:99px; font-size:12px; font-weight:700; margin-bottom:20px;">
					✨ 八木仁平方法论
				</div>
				<h1>不要只是努力，<br /><span class="text-indigo">做出正确的选择</span></h1>
				<p style="color:#475569; max-width:500px; margin:0 auto; font-size:1.05rem;">
					AI 助手将引导你生成属于你的“人生使用说明书”。
				</p>
			</section>

			<!-- Formula -->
			<section class="glass-card">
				<h2 style="text-align:center; margin-top:0; font-size:1.5rem; color:#334155;">人生之路公式</h2>
				<div class="formula-grid">
					<div class="formula-item">
						<div style="font-size:1.5rem;">❤️</div>
						<h3 style="margin:5px 0 0; color:#be123c;">热情</h3>
						<p style="font-size:12px; color:#881337; margin:0;">喜欢的</p>
					</div>
					<div class="multiply">×</div>
					<div class="formula-item">
						<div style="font-size:1.5rem;">⚡</div>
						<h3 style="margin:5px 0 0; color:#b45309;">才能</h3>
						<p style="font-size:12px; color:#78350f; margin:0;">擅长的</p>
					</div>
					<div class="multiply">×</div>
					<div class="formula-item">
						<div style="font-size:1.5rem;">💎</div>
						<h3 style="margin:5px 0 0; color:#0e7490;">价值观</h3>
						<p style="font-size:12px; color:#155e75; margin:0;">在乎的</p>
					</div>
				</div>
			</section>

			<!-- Video -->
			<div class="video-card">
				<h3 style="margin-top:0;">找到你的“人生之路”</h3>
				<p style="color:#a5b4fc; font-size:0.9rem; margin-bottom:20px;">老高与小茉 Mr & Mrs Gao</p>
				<a href="https://b23.tv/Pedon5x" target="_blank" class="video-btn">
					📺 观看视频
				</a>
			</div>

			<!-- CTA -->
			<section class="cta-section">
				<h2 style="margin:0 0 10px; font-size:1.6rem;">准备好了吗？</h2>
				<p style="opacity:0.9; font-size:1rem;">不需要面对白纸发愁。</p>
				<a href="/guide" class="cta-btn">🚀 开始探索</a>
			</section>

		</main>
		<Footer />
	</body>
</html>
